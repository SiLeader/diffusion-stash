import { DefaultImage } from './default-image';

describe('DefaultImage', () => {
  it('should create an instance', () => {
    const directive = new DefaultImage();
    expect(directive).toBeTruthy();
  });
});
