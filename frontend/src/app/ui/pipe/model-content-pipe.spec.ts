import { ModelContentPipe } from './model-content-pipe';

describe('ModelContentPipe', () => {
  it('create an instance', () => {
    const pipe = new ModelContentPipe();
    expect(pipe).toBeTruthy();
  });
});
